# 极简MVP技术方案 - 对标RunningHub

## 📋 项目概述

**项目名称**：ImageGen（暂定）
**项目定位**：极简AI图像生成应用
**核心功能**：提示词 → 模型选择 → 图片生成 → 结果展示
**目标用户**：普通用户、设计师、内容创作者
**开发周期**：4-6周（MVP版本）
**开发成本**：极低（单人开发可行）

---

## 🎯 核心功能定义

### MVP功能范围（必做）
```
✅ 用户认证系统
   - 注册/登录
   - 基础用户信息管理

✅ 提示词输入
   - 简单文本输入框
   - 基础提示词建议

✅ 模型选择
   - 预设3-5个基础模型
   - 模型快速切换

✅ 图片生成
   - 调用ComfyUI后端
   - 实时进度反馈
   - 生成结果展示

✅ 结果管理
   - 生成历史记录
   - 图片下载
   - 基础分享功能
```

### 暂不支持（后期迭代）
```
❌ 复杂工作流编辑
❌ 节点市场/应用商店
❌ ControlNet/LoRA等高级控制
❌ 模型训练
❌ 创作者经济/分成
❌ 实时协作
❌ 离线支持
```

---

## 🛠️ 技术栈选择

### 前端技术栈
```
框架：Flutter 3.x
语言：Dart
状态管理：Provider / Riverpod
HTTP客户端：Dio
本地存储：Hive / SharedPreferences
图片处理：image_picker, cached_network_image
UI组件：Material Design 3

优势：
- 跨平台（iOS/Android）
- 性能优秀
- 开发效率高
- 热重载支持
```

### 后端技术栈
```
框架：FastAPI
语言：Python 3.10+
ORM：SQLAlchemy
数据库：PostgreSQL
缓存：Redis
任务队列：Celery + Redis
ComfyUI集成：Python SDK

优势：
- 开发速度快
- 异步支持
- 自动API文档
- 与ComfyUI无缝集成
```

### 基础设施
```
数据库：PostgreSQL 14+
缓存：Redis 7.x
文件存储：本地存储 / 对象存储（可选）
ComfyUI：自建或云端部署
云服务：阿里云/腾讯云（可选）
```

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────┐
│         移动应用 (Flutter)              │
│  ┌─────────────────────────────────┐   │
│  │ 提示词输入 → 模型选择 → 生成    │   │
│  └─────────────────────────────────┘   │
└────────────────┬────────────────────────┘
                 │ HTTPS/REST API
                 ▼
┌─────────────────────────────────────────┐
│      后端服务 (FastAPI)                 │
│  ┌─────────────────────────────────┐   │
│  │ 认证 | 用户 | 生成任务 | 结果   │   │
│  └─────────────────────────────────┘   │
└────────────────┬────────────────────────┘
                 │
    ┌────────────┼────────────┐
    ▼            ▼            ▼
┌────────┐  ┌────────┐  ┌──────────┐
│PostgreSQL│ │ Redis  │  │ComfyUI   │
│ 数据库   │ │ 缓存   │  │ 执行引擎 │
└────────┘  └────────┘  └──────────┘
```

### 核心模块设计

#### 前端模块 (Flutter)
```
lib/
├── screens/
│   ├── auth/
│   │   ├── login_screen.dart
│   │   └── register_screen.dart
│   ├── home/
│   │   └── home_screen.dart
│   ├── generate/
│   │   ├── prompt_input_screen.dart
│   │   ├── model_selection_screen.dart
│   │   └── generation_progress_screen.dart
│   ├── result/
│   │   ├── result_display_screen.dart
│   │   └── history_screen.dart
│   └── profile/
│       └── profile_screen.dart
├── providers/
│   ├── auth_provider.dart
│   ├── generation_provider.dart
│   └── user_provider.dart
├── services/
│   ├── api_service.dart
│   ├── storage_service.dart
│   └── image_service.dart
├── models/
│   ├── user.dart
│   ├── generation_task.dart
│   ├── model.dart
│   └── result.dart
└── widgets/
    ├── common/
    └── custom/
```

#### 后端模块 (FastAPI)
```
backend/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── api/
│   │   ├── auth.py
│   │   ├── users.py
│   │   ├── generation.py
│   │   └── results.py
│   ├── models/
│   │   ├── user.py
│   │   ├── generation_task.py
│   │   └── result.py
│   ├── schemas/
│   │   ├── user.py
│   │   ├── generation.py
│   │   └── result.py
│   ├── services/
│   │   ├── auth_service.py
│   │   ├── generation_service.py
│   │   ├── comfyui_service.py
│   │   └── storage_service.py
│   ├── tasks/
│   │   └── celery_tasks.py
│   └── utils/
│       ├── security.py
│       └── validators.py
├── requirements.txt
└── docker-compose.yml
```

---

## 📊 数据库设计

### 核心表结构

#### users 表
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    avatar_url VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### generation_tasks 表
```sql
CREATE TABLE generation_tasks (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    prompt TEXT NOT NULL,
    model_name VARCHAR(100) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',  -- pending, processing, completed, failed
    result_id INTEGER REFERENCES results(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP
);
```

#### results 表
```sql
CREATE TABLE results (
    id SERIAL PRIMARY KEY,
    task_id INTEGER NOT NULL REFERENCES generation_tasks(id),
    image_url VARCHAR(255) NOT NULL,
    image_path VARCHAR(255),
    metadata JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### models 表
```sql
CREATE TABLE models (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) UNIQUE NOT NULL,
    display_name VARCHAR(100),
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔌 API接口设计

### 认证接口
```
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
```

### 用户接口
```
GET /api/v1/users/me
PUT /api/v1/users/me
POST /api/v1/users/me/avatar
```

### 生成接口
```
POST /api/v1/generation/generate
  请求：{prompt, model_name}
  响应：{task_id, status}

GET /api/v1/generation/tasks/{task_id}
  响应：{task_id, status, result}

GET /api/v1/generation/history
  响应：[{task_id, prompt, model, status, created_at}]
```

### 模型接口
```
GET /api/v1/models
  响应：[{id, name, display_name, description}]
```

### 结果接口
```
GET /api/v1/results/{result_id}
  响应：{image_url, metadata}

DELETE /api/v1/results/{result_id}
```

---

## 🔄 工作流设计

### 用户生成图片的完整流程

```
1. 用户输入提示词
   ↓
2. 选择基础模型
   ↓
3. 点击"生成"按钮
   ↓
4. 前端发送请求到后端
   POST /api/v1/generation/generate
   ↓
5. 后端创建任务记录
   ↓
6. 后端将任务加入Celery队列
   ↓
7. Celery Worker处理任务
   - 调用ComfyUI API
   - 执行工作流
   - 获取生成结果
   ↓
8. 保存结果到数据库
   ↓
9. 前端轮询查询任务状态
   GET /api/v1/generation/tasks/{task_id}
   ↓
10. 任务完成，返回图片URL
   ↓
11. 前端展示结果
   ↓
12. 用户可以下载/分享
```

---

## 🚀 部署架构

### 开发环境
```
本地开发：
- Flutter开发环境
- Python虚拟环境
- PostgreSQL本地实例
- Redis本地实例
- ComfyUI本地部署
```

### 生产环境
```
选项1：云服务器部署（推荐）
- 阿里云/腾讯云 ECS
- RDS PostgreSQL
- Redis缓存
- 对象存储（OSS/COS）
- ComfyUI容器化部署

选项2：Docker Compose本地部署
- 所有服务容器化
- 一键启动
```

---

## 💰 成本估算

### 开发成本
```
前端开发：40-50小时
后端开发：30-40小时
数据库设计：5-10小时
测试和优化：20-30小时
部署和文档：10-15小时
─────────────────
总计：105-145小时（2-3周）
```

### 运营成本（月）
```
云服务器：50-100元
数据库：30-50元
存储：10-20元
带宽：20-50元
─────────────────
总计：110-220元/月
```

---

## 🔒 安全考虑

### 认证和授权
- JWT Token认证
- 刷新Token机制
- 权限验证

### 数据安全
- 密码加密存储
- HTTPS通信
- 敏感数据加密

### API安全
- 速率限制
- 输入验证
- SQL注入防护

---

## 📈 性能目标

| 指标 | 目标 |
|------|------|
| 登录响应时间 | < 2秒 |
| 生成请求响应 | < 1秒 |
| 图片生成时间 | 30-120秒（取决于模型） |
| 结果查询响应 | < 500ms |
| 并发用户数 | 10-50（MVP阶段） |

---

## 🎓 技术栈总结

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端** | Flutter 3 | 跨平台移动应用 |
| | Dart | 编程语言 |
| | Provider | 状态管理 |
| **后端** | FastAPI | 高性能API框架 |
| | Python 3.10+ | 编程语言 |
| | SQLAlchemy | ORM框架 |
| **数据库** | PostgreSQL | 主数据库 |
| | Redis | 缓存和任务队列 |
| **任务处理** | Celery | 异步任务队列 |
| **AI引擎** | ComfyUI | 图像生成引擎 |
| **部署** | Docker | 容器化 |
| | Docker Compose | 容器编排 |

---

## ✨ 技术亮点

### 1. 极简架构
- 最小化功能集合
- 快速迭代
- 易于维护

### 2. 异步处理
- Celery任务队列
- 不阻塞用户请求
- 支持长时间运行的生成任务

### 3. 跨平台支持
- Flutter支持iOS/Android
- 一套代码多平台运行

### 4. 高效开发
- FastAPI自动文档
- 热重载支持
- 快速原型开发

### 5. 成本优化
- 开源技术栈
- 最小化云服务依赖
- 可本地部署

---

## 📝 总结

这是一个**极简MVP版本**的技术方案，核心特点：

- **功能极简** - 只做提示词→模型→生成→展示
- **技术成熟** - 使用成熟的开源技术栈
- **开发快速** - 2-3周可完成MVP
- **成本低廉** - 月运营成本100-200元
- **易于扩展** - 为后期功能迭代预留接口

**下一步**：制定详细的开发计划和里程碑。


